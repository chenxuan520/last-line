# Architecture

## Runtime Flow

`GameApp` loads the asset catalog, restores settings, and exposes two isolated session paths. `BattleRoyaleSession` remains the local single-player authority. `MultiplayerSession` owns only input, prediction, interpolation, Babylon presentation, HUD, and a WebSocket connection; it never creates a `GameSimulation`.

Each fixed step follows this order:

1. Controllers produce `ActorCommand` values.
2. Movement commands update authoritative positions and deployment state.
3. Inventory actions resolve with tick-rotated conflict ordering.
4. Accepted one-shot throwable commands consume backpack grenades and create authoritative projectiles.
5. Combat intents are collected and gunfire damage is settled as a batch.
6. Active grenades advance with fixed substeps, authoritative collision, fuse expiry, explosion obstruction, and batched radial damage.
7. `BattleRoyaleMode` advances flight, safe zones, zone damage, and results.
8. Dead inventories become reusable ground-loot records.
9. Rendering synchronizes from the resulting state.

In multiplayer, the same steps run inside one platform-hosted `GameRoom` at 30 Hz. Cloudflare assigns one Durable Object per room; standalone assigns one in-process room service backed by local SQLite. Browsers use the same protocol in both modes and receive a full initial state followed by 10 Hz actor frames, loot changes, sequenced events, and input acknowledgements. Input frames may also carry the monotonic server tick currently represented by remote interpolation; the server bounds that value to ticks previously sent to the connection.

## Authoritative Rules

`src/game/` has no DOM or Babylon dependency. `MatchState` contains serializable actors, inventories, ground loot, active frag grenades, flight, safe zone, phase, and result data.

`SimulationCombatWorld` performs hitscan, line-of-sight, throwable-sweep, and explosion-obstruction tests against fixed actor capsules, generated wall segments, roof caps, ramps, authoritative tree trunks, and shared terrain data. Frag grenades use fixed 30Hz substeps, finite sphere sweeps, bounce damping, a 3.5-second fuse, an 8m capsule-distance falloff, self-damage, and current-map obstruction. Tree-trunk positions and dimensions are regenerated from `mapSeed`, shared by single-player, server authority, and every client, and are also consumed by movement, Bot navigation, retreat-cover selection, dynamic-loot placement, and grenades. Babylon meshes only present the state and never decide a hit; foliage remains visual-only.

Actor dimensions, protected-damage calculation, ground-loot interaction/placement values, and the 30 Hz simulation cadence each have one shared game-domain definition. Movement, combat, navigation, AI, prediction, server scheduling, map generation, and presentation consume those contracts instead of carrying independent numeric copies.

Simultaneous lethal damage uses a deterministic tick-based selector. The selected survivor still receives normal damage, armor reduction, and events, with health clamped to 1 only when every remaining actor would otherwise die in the same tick.

Damage phases have an explicit sub-tick contract: collected gunfire settles first, expired grenade explosions settle second, and battle-royale safe-zone damage settles last. Insertion order and actor kind remain irrelevant inside each phase.

## Controllers

`HumanController` maps keyboard, mouse, and touch state to the same `ActorCommand`. Desktop `3` selects a carried frag grenade, primary-fire hold previews the trajectory, release emits one `throwGrenade`, secondary fire toggles high/low throw, and weapon selection cancels without consumption. Touch exposes a dedicated grenade count/action and reuses both fire holds, including right-side drag-to-aim; pointer cancellation cancels preparation rather than throwing. While desktop Tab is held, wheel input is routed to the scrollable leaderboard before spectator/weapon switching; releasing Tab restores the historical wheel behavior. `TouchInputAdapter` only tracks bounded Pointer Event state for the movement joystick, look region, at most two fire holds, and one-shot HUD actions; it never reads or mutates authoritative match state. The mobile backpack action only opens the existing HUD inventory view and never creates a second inventory model. Portrait rotation, page hiding, pause, healing, and disposal clear held touch state to prevent stuck movement, fire, or grenade preparation. `MobileFullscreenController` requests fullscreen synchronously from a real start/retry click, then attempts a landscape orientation lock when supported. It never requests from `orientationchange`; rejected, unsupported, or exited fullscreen falls back to manual rotation and a non-blocking HUD retry action.

Each `BotController` has independent decision timers and memory. Bots use the same commands and systems as the player for movement, looting, firing, reloading, switching weapons, healing, and frag grenades. Grenade evaluation is staggered and bounded to visible 8–32m targets, one high-arc solve, landing error/self-distance checks, and a 12–20-second cooldown. The authoritative simulation admits at most six active AI-controlled grenades across regular Bots and disconnected-human takeovers.

`InventorySystem` remains authoritative for all drops. Human controllers encode the selected backpack index and expected item ID into the existing one-shot `dropItem` command; the rule system validates both before dropping the entire stack. Desktop slots use keys `4`–`9`, while mobile slots expose HUD drop buttons. Full backpacks continue rejecting new stacks until the player explicitly frees a slot, and bot looting policy remains unchanged.

Local development may explicitly enable `npm run dev -- --debug=true`. `GameApp` consumes the compile-time flag only to show the menu badge and load the debug-only stylesheet; `BattleRoyaleSession` alone instantiates the debug panel, mutation system, and immunity policy. Multiplayer sessions and server authority never consume debug behavior. The single-player debug panel emits bounded actions to a DOM-free debug system for immediate landing, property changes, legal `ITEMS.maxStack` inventory grants, and test loadouts. One injected `DamageSystem` immunity policy is shared by the local session's gunfire, grenade, and safe-zone paths so the debug player is consistently invulnerable without protecting bots.

## Rendering

`IslandScene` builds the 2400m island from per-match seeded map points: eight irregular named POIs, eight wilderness compounds, nearest-neighbor roads, randomly scattered enterable buildings, variable-density loot, 384 authoritative tree trunks with visual foliage, dense natural details, procedural actors, a terrain-following safe-zone ribbon, and weapon/grenade first-person models. Every map preserves its existing 240 base and 10 supplemental medical loot points, then appends exactly 10 deterministic grenade points containing two frag grenades each. Ground loot optionally uses 15 shared low-poly item geometry templates plus one fallback; each loot record still owns one reusable mesh and switches shared geometry/material in place when its record generation changes. `MapLayout.hospital` deterministically reuses one existing building as a white two-story hospital, adds a visual-only medical cross and minimap marker, and keeps its two supplemental medical records before the grenade range. The player camera applies scope FOV only while a sniper is active and the right mouse button is held or touch ADS is toggled.

Map identity is explicit and independent from the random seed. `mapId: "island" | "town" | "mixed"` selects the layout family while `mapSeed` only varies that family; legacy data without a map ID normalizes to the island. The Greyfurnace City (`town`) layout uses the same 2400m boundary with 448 dense industrial buildings, 54 four/five-story structures, 32 second-story skybridges, a map/seed-dependent initial loot count, and one connected seeded road graph. Five bounded-jitter arterial bands in each direction form the reliable backbone; seeded local segments terminate as T junctions or merge logical development cells into larger visual blocks. Each seed also varies dense/mixed/industrial parcel profiles, setbacks/courtyards, POI anchors, roadside cover, trees, and visual details while keeping all footprints clear of the shared road shoulder. High-rise floors, openings, ramps, bridge decks, rails, collision, line of sight, navigation, server authority, and rendering consume the same `MapLayout`.

The Cinder Mist County (`mixed`, displayed as `烬岚郡`) layout keeps the 2400m boundary and places six enlarged 780m near-square named macro regions in a compact irregular seeded cluster. The centers vary with `mapSeed`, maintain bounded nearest-neighbor spacing, cannot collapse into fixed rows, columns, or rectangular slots, and keep their bounding-box area at or below 1,450,000m²—at least 14.8% smaller than the former fixed center grid. `赤钟城区`, `风穗乡`, and `沉杉岭` permanently provide one dense town, one sparse rural district, and one mountain forest; the remaining three regions deterministically select town, rural, or forest from `mapSeed` and take a unique name from their type-specific pool. Exactly five shortest eligible connector roads join the six regions without crossing each other or a third region's developed core, while explicit `urbanRoadSegments` remain town-only. Every town has 36 authoritative buildings covering at least 38% of its nearest-owned regional footprint, every rural region has 9 buildings plus 30 hay covers, and every forest has 2 cabins, 180 authoritative tree trunks, 24 rocks, and three hills. Because neighboring regional footprints may meet in natural transition bands, buildings, trees, rocks, hay, and regional loot carry explicit `regionId` ownership and must also be nearest to their owning center; rectangular containment alone never determines ownership. Type-specific landing zones use town streets, rural roads, or forest clearings rather than assuming every label center is navigable. Every terrain hill fits its full radius inside the 2400m map boundary. Forest tree and rock footprints accept at most 0.798m sampled terrain variation and center their base between the footprint minimum and maximum, keeping either embedding or floating strictly below 0.4m after millimeter coordinate rounding while preserving dense sloped cover. The sole white hospital is always inside the fixed town and is displayed simply as `医院`. The blueprint owns 16 landing zones, 240 regional loot points, 10 supplemental medical points, and 10 separately appended grenade points; each mixed outdoor loot point stays near its own landing-zone anchor, keeps 8m from building edges and 14m from natural blockers, and requires a clear authoritative corridor back to that anchor. Walls, doors, floors, ramps, terrain, roads, trees, rocks, hay, loot, Movement, Combat/LOS, GridNavigator, AI, Worker/standalone authority, minimap, and rendering consume the same seeded facts. High-quality industrial details key off actual `townKind` buildings and only the blueprint's urban-road subset, so macro connectors, rural districts, and forests never inherit city-wide decoration.

All three map families also select one ammunition depot distinct from the hospital. It uses an explicit authoritative building record with explicit loot indexes for every interior story. The original 240 regional records plus 10 medical records remain the 250-record global loot budget; each depot story appends `ammo.rifle ×90`, `ammo.light ×96`, `ammo.shell ×18`, and `ammo.sniper ×16` as four additional ordinary `GroundLootState` records, followed by the 10 grenade records. The complete canonical initial count is therefore `250 + storyCount × 4 + 10`: one- through four-story depots produce `264 / 268 / 272 / 276` records. Every point rests on its story's authoritative support, remains outside the stairwell footprint, is reachable from the ground-floor door through the same internal ramps, and uses the same pickup, AI, replication, checkpoint, and reuse paths as any other loot.

Every building now owns an internal stairwell, including one-story houses. Each building contributes exactly `storyCount` authoritative internal ramps, with the final ramp reaching a roof opening; floor and roof slabs leave the matching stairwell aperture and landing. No map emits an exterior scaffold ramp. Movement support, combat intersection, GridNavigator door/vertical routing, loot clearance, and Babylon ramp meshes all consume this same internal-only geometry.

Every island, town, and mixed building also selects one of six deterministic architectural profiles from `mapId + mapSeed + buildingId`. The profile adds varied parapet heights, corner or facade piers, cornices, service screens, split roof monitors, or stepped front edges. These substantial silhouettes are ordinary authoritative `wallSegments`, render on every quality level, and are consumed by Movement, combat/LOS, throwable sweeps, GridNavigator, server authority, and Babylon from the same records. The roof remains a flat authoritative support reached by the internal stair; profile geometry stays clear of that opening and retains one east-side escape gap so existing roof-drop play remains possible. Static loot generation continues to use the pre-profile facade geometry, preserving seeded loot positions.

Every map and quality level keeps the baseline clear color, fog, ambient light, sunlight, and disabled Babylon image processing; high quality does not apply a scene-wide tone-mapping, exposure, contrast, or vignette filter. High-quality island presentation adds visual-only shore foam, road wetness, and POI light strips. High-quality Greyfurnace presentation adds road edges and markings, wet patches and cracks, transparent facade panes, industrial lights, small rooftop equipment, door-derived loading-bay frames and canopies, street lamps, facade pipes, brackets, overhead cables, narrow runoff/rust accents, smokestacks, gantries, elevated pipes, and smoke. These small meshes are non-pickable and non-colliding and never replace authoritative walls, openings, floors, bridges, architectural profiles, movement support, combat occlusion, or navigation geometry. Town panes use a dedicated transparent material; the aircraft cockpit retains the baseline opaque building-window material.

Desktop match entry requests pointer lock only from real user activation. Single-player deploy, public matchmaking/public room entry, private ready/start, and the corresponding session resume paths share the same safe helper: unsupported APIs, synchronous exceptions, legacy void returns, and asynchronous rejection never abort loading. The lock may survive asynchronous room admission, countdown, and scene construction so multiplayer reaches active input without showing the resume card first; menu, lobby failure, room exit, and terminal paths release a held lock. Touch input never requests pointer lock, and the existing `继续游戏` card remains the fallback when a browser refuses activation.

All multiplayer admission UI shares one generation-token single-flight gate. A second Quick Match, room creation, code join, or public-room click is rejected while the first attempt is unresolved; leaving the menu invalidates the token. When a replacement connection is installed, callbacks from the stale connection cannot update status, release fullscreen, or clear pointer lock because every handler checks active connection identity first.

Static visual details are merged by decoration type to bound scene resources. Single-material groups keep one `StandardMaterial`; a town group whose sources intentionally use different materials uses Babylon `MultiMaterial` submeshes so rooftop equipment, pipe brackets, gantries, and elevated pipes retain their distinct material response after merging. High-quality procedural shoulder, knee, and belt gear remains visible with both procedural and loaded base/LOD1 character bodies, while the local first-person view renders weapons without added view-arm meshes.

Optional GLB models are loaded asynchronously and instantiated as non-pickable visual children. Procedural models remain the fallback. Repeated loot drops reuse inactive state IDs and marker meshes, and scene disposal clears marker references and imported containers.

`AssetCatalog` preloads and decode-validates non-model assets once. Scene image textures are created directly from those cached payloads, never by issuing a second URL request, and are marked non-blocking so terrain/building/brand geometry renders immediately from procedural colors and vertex colors while GPU texture upload completes. Missing or failed world-image payloads produce no Babylon texture and retain the same visible geometry fallback; cross-type UI SVG fallbacks are not applied as terrain textures.

World textures are selected through stable semantic asset IDs without entering authoritative state. The island uses gravel/dry-soil terrain, damaged-asphalt roads, weathered plaster/brick/concrete walls, and membrane/gray-tile/red-brown-tile roofs. Greyfurnace uses urban concrete, damaged asphalt, aged concrete/brick facades, and membrane/rusted-metal roofs. Cinder Mist County derives the same presentation-only choice from seeded mixed-region ownership: town regions use the Greyfurnace family, rural regions use dry soil/gravel/sparse-grass mud with plaster/brick and gray/red-brown roofs, and forest regions use humus/wet moss with plaster/concrete and gray roofs. These roles share bounded materials and textures across buildings and quality levels. The hospital remains untextured white, and the ammunition depot retains its dedicated dark industrial surface.

The enemy character palette uses a dark blue-gray uniform while preserving authored skin colors; remote human characters retain their green palette. Procedural third-person weapons are created under each imported character LOD's `weapon_socket`, so reverting the weapon style does not reintroduce the old character coordinate system. Dynamic GLTF loader chunks receive two bounded reload retries across deployment cache mismatches before character fallback is accepted.

`IslandScene` receives an explicit local actor ID for multiplayer. Only that actor uses the first-person hitbox representation; remote human actors use the same third-person presentation contract as AI while retaining `kind: "player"` in authoritative state.

Multi-story buildings use deterministic two-lane switchback ramps with authoritative cross-lane landings. Movement, combat, and navigation consume the same ramp/floor geometry, including paths that begin midway along a ramp. Every map renders the same five decorative brand-sign assets at deterministic clear positions near map-appropriate semantic POIs: the island retains its harbor/radar/warehouse anchors, Greyfurnace uses its worker housing/industrial park/storage harbor, and Cinder Mist County uses its fixed town/forest/rural regions. Signs remain non-pickable, non-colliding presentation meshes, and missing anchors or safe placements fail explicitly instead of silently reducing the set. The menu's responsive ABOUT field manual exposes creator, repository, gameplay, desktop controls, and mobile controls without changing session state.

Result actions preserve session ownership: a completed single-player victory offers both a fresh deployment and a return to the main menu, while multiplayer completion exposes only the existing return-to-online-lobby action and closes the match connection through the session exit callback.

## Multiplayer Services

`LobbyDirectory` owns temporary guest sessions, public room summaries, quick matching, and private room-code lookup. Every room has a separate `GameRoom` service that owns lobby readiness, WebSockets, actor assignment, checkpoints, and one `MatchRuntime`.

Room map selection is immutable after creation. Public/private creation and quick match carry the requested `mapId`; quick match only joins waiting rooms with the same map, direct code joins inherit the room map, and lobby/public summaries display the selected map. Multiplayer protocol version 10 identifies the shared authoritative architectural-profile geometry in addition to the version-9 map, stair/floor, ammunition-depot, and grenade contracts. Clients and servers on protocol 9 must not share rooms because they derive different collision and occlusion from the same map seed.

Checkpoint compatibility first requires version 9, then validates outer sequence counters and the complete recoverable `MatchState` shape. Version 8 rooms are discarded rather than resumed into changed building collision. A recoverable roster contains exactly the configured 50 actors, and each actor record key equals its stable `actor.id`. Its canonical initial loot count is rebuilt from the checkpoint's explicit `mapId + mapSeed`, so every initial global, per-story depot, and grenade key must exist with matching `loot.id` while valid dynamic drop/death records may remain. Every backpack stack references the authoritative `ITEMS` catalog, has a positive integer quantity, and stays within that item's configured `maxStack`. Active grenades require stable sequence IDs, valid owners, finite vectors, positive bounded fuses, persisted AI provenance, and a strictly greater next sequence. A `closed` safe zone is recoverable only in combat/finished state after the production stages' minimum accelerated timeline, at the final configured center, radius, timer, and damage.

Persisted member records are validated as complete `RoomMemberRecord` objects before any lobby or reconnect field is read: the record key must equal `playerId`, required strings and booleans must be present, account ID/session revision must be paired, numeric deadlines/epochs must be valid, and optional tokens/actor IDs must use their declared types. Running/finished room persistence must then contain 2–10 non-null, unique actor IDs exactly equal to the checkpoint's complete `kind: "player"` actor set; a member cannot point to a bot, share an actor, or leave a player actor unbound. Missing, null, array, partial, malformed, unknown-map, version 8, and older state are removed through the same Worker/standalone incompatible-state cleanup path instead of throwing during startup. Earlier versions are rejected because map, stair/depot, grenade, and building-collision histories cannot be disambiguated safely.

The directory classes extend the platform-neutral `DurableService` base and consume narrow storage, namespace, alarm, task, and socket interfaces. Cloudflare supplies those capabilities through Durable Object state. `standalone/LocalDurableObjectRuntime.ts` supplies the same contracts with one Node.js process, local SQLite key/value and SQL tables, local timers, and `ws` sockets. The gateway, room transitions, admission/reconnect rotation, account rules, administrator rules, protocol parsing, and authoritative match runtime are therefore shared rather than copied. `SERVER_PLATFORM` only selects platform-specific origin defaults; clients do not branch on the backend type.

`AccountDirectory` uses the platform SQL store for persistent player accounts: Durable Object SQLite on Cloudflare and the standalone database on Node. It implements case-insensitive unique usernames, native WebCrypto PBKDF2-SHA-256 password records at 100,000 iterations, opaque access/refresh sessions, refresh rotation, logout, account lookup, password changes, disabling, and session revocation. Public auth routes expose only short-lived access tokens; refresh tokens remain in `Secure`, `HttpOnly`, `SameSite=Strict`, host-only cookies.

`AdminDirectory` owns one administrator identity, eight-hour opaque Cookie sessions, and the global multiplayer admission policy. The shared gateway serves a same-origin `/admin` terminal. Bootstrap and forgotten-password recovery use separate deployment secrets; every mutation requires same-origin requests, and internal account/room operations require a separate in-process/generated or Worker capability. Optional Turnstile validation becomes fail-closed only when both the site and secret keys are configured.

When registration/login is required, the gateway validates the player access token before creating a guest-compatible room identity. That identity retains the account ID and session revision; Lobby and GameRoom revalidate linked accounts so disabling or revoking an account invalidates matchmaking, admissions, reconnects, and active sockets. Toggling the global policy does not interrupt pre-existing unlinked guest rooms.

`MatchRuntime` reuses `GameSimulation`, `BattleRoyaleMode`, `SimulationCombatWorld`, and `BotController`. Matches contain 2–10 stable human actors and enough bots to total 50. AI decisions are distributed across three deterministic cohorts while movement and all authoritative systems remain 30 Hz. Disconnected humans receive idle input, then server-side bot takeover without changing actor identity.

`CommandInbox` rejects stale sequences, expires continuous input, and consumes jump, interact, reload, switching, item use, drops, and grenade throws only once. The client predicts local movement and reversible local firing presentation, then replays unacknowledged movement after each server correction. Predicted firing is limited by the visible weapon, reload, magazine, configured cadence, and absence of a grenade throw; it immediately produces local audio, recoil, muzzle flash, and a no-impact tracer. Authoritative shot events reconcile that presentation without replaying it. Grenade inventory consumption, creation, flight, bounce, explosion, damage, death, and resulting loot remain server-owned; clients only preview their own path and interpolate bounded replicated grenade meshes.

Ground loot is replicated in a 400m horizontal footprint while an actor is in the aircraft or parachuting, then contracts to 60m after landing. Initial/full state seeds the per-player visible set; subsequent frames send only newly visible, dirty-visible, and newly hidden records, with unavailable tombstones preserving bounded client mesh reuse. Pickup and interaction checks remain three-dimensional and authoritative.

For human hitscan only, `MatchRuntime` retains a short ring of actor capsule poses. A client render tick is optional for backward compatibility, must be monotonic, cannot exceed the newest tick sent on that socket, and is clamped to 200ms behind the current server tick. `LagCompensatedCombatWorld` queries actor capsules from that bounded frame while continuing to test terrain, buildings, ramps, rocks, cover, and tree trunks against the authoritative current map. Missing, stale-after-restart, or legacy tick metadata falls back to current-world hit testing. Bots and all single-player shots continue to use current state directly.

`MultiplayerSession` keeps authoritative and presentation state separate during reconciliation. A snapshot replaces the authoritative state, then unacknowledged local movement inputs are replayed. Grounded corrections up to 6m are hidden only by a temporary camera/visual offset; aircraft and parachute corrections use bounded speed/tick-aware budgets so normal 10Hz flight does not alternate between interpolation and snaps. The external aircraft uses the rendered server-tick timeline instead of raw snapshot progress. These offsets never mutate `MatchState`, commands, hit tests, pickup distance, or server acknowledgement state. Full resync, death, deployment changes, and implausible teleports still snap immediately.

Human disconnect/reconnect transitions share the sequenced snapshot event stream with gameplay events. `MatchRuntime` emits at most one event per real state transition, stale replacement-socket closes are rejected by connection epoch, restore is silent, and clients resolve only known display names. Desktop uses the normal top-right feed; touch layouts use a compact connection-only feed. Exiting a running multiplayer pause card closes the socket and returns to matchmaking; it does not send the waiting-room-only `lobby.leave` command.

Remote positions use per-actor transitions starting from the position that was actually rendered before the snapshot. Old transitions advance before queued messages are consumed, so a long render frame that occurred before snapshot receipt cannot instantly complete the new transition. Duration follows the authoritative tick gap and is clamped to 120–250ms. A parallel scalar transition tracks the approximate server tick represented by those rendered poses and is attached to outgoing input for bounded hitscan rewind. Newly visible actors, alive/deployment changes, missing previous state, and impossible authoritative movement snap immediately. Grounded actors have a 6m smoothing ceiling. Parachuting actors use a dynamic ceiling derived from the real tick gap and the authoritative maximum glide/descent speed, with an 18m hard cap, so valid high-speed gliding stays smooth without disguising teleports.

`GameRoom` still advances rules at 30 Hz and considers a snapshot every third tick. An 80ms monotonic minimum interval suppresses back-to-back frames while the scheduler catches up after a stall; normal cadence remains approximately 10 Hz. `MatchRuntime.takeFrame()` is called only for frames that are actually sent, so sequenced events and dirty loot accumulate until the next frame. Match completion always forces a final frame even when the throttle suppressed that tick's regular candidate.

Standalone stores guests, room metadata, admissions, reconnect credentials, account/admin data, deadlines, and checkpoints in one WAL-mode SQLite database. A separate SQLite exclusive lock automatically releases its OS lock on process death and prevents two live Node processes from advancing the same data directory. Alarm rows remain durable until their handler completes and use generations so a reschedule cannot be deleted by the preceding invocation. On startup, persisted alarm records instantiate their services; running rooms rebuild `MatchRuntime` from the newest compatible, versioned checkpoint and immediately resume. A room whose checkpoint predates the current authoritative map semantics is closed and deleted instead of mixing old actor/loot positions with new collision geometry. Dormant room services are evicted after their persistent state, alarm, and sockets are gone, so completed matches do not accumulate in memory.

Reconnect rotation is two-phase: the previous token remains valid while a replacement is pending, and `connection.ack` promotes only the token issued to that connection. Losing the socket before `welcome` or its acknowledgement therefore cannot strand a client. Graceful SIGINT/SIGTERM stops room loops and writes an early checkpoint before bounded network draining, then writes again after sockets settle; the current checkpoint contract can still lose at most the interval since the last crash-safe write after an ungraceful kill and does not promise bit-for-bit restoration of transient Bot/controller memory.

Server telemetry is deliberately observational. `LobbyDirectory` emits an absolute `active_rooms` gauge after persisted room-directory changes; each `GameRoom` emits 60-second count/sum/max summaries for scheduler `tick_delay_ms`, outbound `websocket_buffered_bytes`, and serialization-through-storage `checkpoint_duration_ms`. Records use the fixed `server_metric` schema version 1 and contain no room, account, IP, URL, admission, or reconnect identifiers. Standalone writes JSON lines to stdout and reports the real `ws.bufferedAmount`; Cloudflare writes the same structured records to Workers Logs but marks buffering samples unavailable because its server WebSocket API exposes no queue size. Metrics are never stored in room state or checkpoints, sink failures are contained, and no public metrics endpoint exists. `/health` keeps its liveness-only JSON body; the Worker adds a no-store protocol-version header so deployment smoke can wait for custom-domain propagation before creating any room.

## Performance Strategy

- Fixed 30 Hz rules with decoupled rendering
- Staggered AI decisions based on distance
- Bounded multi-wall path search with per-Bot path reuse
- Shared materials and hardware instances for static trees, shrubs, and decorative rocks; reusable loot records keep individually mutable meshes
- Low/medium/high quality profiles keep all authoritative geometry—including tree trunks, walls, openings, floors, ramps, and skybridges—at identical seeded positions. Profiles vary foliage tessellation, decorative-rock/shrub density, hardware scaling, and 60/90/120 FPS ceilings; expensive island/town-capable presentation batches, procedural character gear, and the enhanced HUD treatment are high-quality-only. Coarse-pointer rendering combines the selected hardware-scaling profile with up to 2x device pixel ratio, improving high-DPI mobile clarity without changing desktop rendering or allowing unbounded render-target growth. Low quality keeps procedural characters without downloading GLBs; medium/high load character GLBs on demand and use distance-based character LOD. Held weapons remain procedural at every quality level.
- HUD state-heavy work runs at 10 Hz while scope, pause, orientation, and touch-control feedback remain render-frame responsive; leaderboard DOM rebuilds only when rank fields change
- Safe-zone geometry reuses one updatable position buffer instead of reallocating vertex/normal arrays during shrinking
- No dynamic shadows or full rigid-body simulation
- Dynamic GLTF loader chunk only when a manifest entry uses GLB
- Active multiplayer rooms use one single-threaded room authority—one Durable Object or one standalone in-process service—with 30 Hz rules, 10 Hz snapshots, and one-second checkpoints
- Multiplayer snapshot smoothing and immediate local firing are presentation-only; authoritative movement, inventory, safe-zone, and result state are never interpolated or rewound, while human hitscan may query at most 200ms of server-owned actor capsule history
- Cloudflare sockets may hibernate through the Durable Object API; standalone sockets remain in the single Node process and recover through reconnect after a restart
- CI budgets raw browser/Worker/server bytes and deterministic AI operations, snapshot bytes, and production-scene resources. These gates avoid machine-dependent timing, FPS, memory, and compression assertions.

## Coverage Baseline

Coverage follows source ownership so every production TypeScript file is counted once: unit tests measure `src/`, Cloudflare integration tests measure `worker/`, and standalone integration tests measure `standalone/`. Node-based suites use V8 coverage; the Workers Vitest pool uses Istanbul instrumentation because `workerd` does not expose native V8 inspector coverage. Declaration files are the only source-pattern exclusion.

`npm run test:coverage` runs all three suites, stores transient JSON summaries under `node_modules/.cache/coverage/`, enforces each suite's checked-in statements/branches/functions/lines thresholds, and prints a count-weighted overall result. The initial measured percentages are:

| Scope | Statements | Branches | Functions | Lines |
| --- | ---: | ---: | ---: | ---: |
| `src/` | 73.93% | 67.33% | 75.86% | 76.14% |
| `worker/` | 76.31% | 69.27% | 91.38% | 82.21% |
| `standalone/` | 76.61% | 61.48% | 86.30% | 79.84% |
| Weighted total | 74.48% | 67.47% | 79.38% | 77.32% |

The gates intentionally round down from those measurements instead of claiming an arbitrary target. Coverage is a regression signal, not a substitute for deterministic behavior/resource budgets, real Worker/standalone contracts, browser acceptance, or code review. Raising thresholds should accompany real tests; lowering them requires an explicit review and must not be achieved by excluding difficult business modules.

## Boundaries

Single-player never opens a network connection and pauses when desktop pointer lock or touch input is inactive. Multiplayer is server-authoritative and continues while a touch client is paused, hidden, or portrait-oriented; that client settles to idle input. Mobile gameplay requires landscape orientation. Fullscreen and orientation lock are best-effort browser capabilities and always retain a manual fallback. Standalone deployment is intentionally limited to one server and one Node process; running several processes against one database is rejected rather than risking duplicate room authorities. There are no social features, rankings, client-authoritative damage, projectile rewind, or speculative future 5v5 rules.
