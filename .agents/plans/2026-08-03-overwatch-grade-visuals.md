## Plan

### Goal

Create a separate branch for a large presentation upgrade that pushes Last Line toward a more polished, high-fidelity, hero-shooter style 3D experience while preserving authoritative gameplay. The change must stay in presentation/UI layers unless a documented contract requires otherwise.

### Scope

- Keep `src/game/` untouched by DOM/Babylon concerns.
- Preserve all island/town map authority, movement, combat, LOS, AI, multiplayer protocol, and server semantics.
- Improve real-time visual quality through Babylon scene settings, camera exposure, fog, tone mapping, material response, static visual detail, and HUD presentation.
- Avoid large new downloads or fragile external assets in this pass; use existing assets, procedural detail, and Babylon features.
- Keep quality settings meaningful, with expensive presentation only enabled where safe.

### Acceptance

- The scene should read as a richer 3D battlefield rather than flat procedural boxes: better lighting, atmospheric depth, varied materials, industrial rooftop/facade detail, road and ground visual breakup, and clearer first-person presentation.
- User explicitly raised the bar after the first pass: the final result must aim for an extremely good-looking, as-real-as-possible 3D industrial town scene. Merely adding decorative boxes is not sufficient; browser screenshot inspection must judge whether the result reads as a believable battlefield with material depth, lighting atmosphere, and dense environmental detail.
- The UI should feel more like a modern shooter HUD without adding instructional text or changing control semantics.
- Presentation additions must be non-pickable/non-colliding unless they are existing authoritative geometry.
- Unit/type/build/budget checks must pass or any environment gap must be recorded.
- If browser verification is used, volume must be set to `0`; pages/contexts and local servers must be closed immediately afterward.
- A reviewer must complete naturally before commit/push.

## Build

- 2026-08-03: Pulled latest `main` with `git pull --ff-only`; repository was already up to date except tag `v0.1.1`. Created branch `visual/overwatch-grade-scene`.
- 2026-08-03: Implemented presentation-only scene upgrades in `IslandScene`: ACES tone mapping, tuned fog/exposure/contrast, town-biased lighting, reflective window panes, industrial lightboxes, road markings/wet patches, rooftop equipment, and first-person view arms. All new town detail is merged into static visual batches and marked non-pickable/non-colliding.
- 2026-08-03: Added NullEngine tests for image-processing configuration, view-arm safety, and town visual batch contracts. `npm run typecheck` passed. Targeted `npm run test:unit -- tests/unit/islandScene.test.ts` passed: 19 tests in 102.94s.
- 2026-08-03: Attempted full `npm run test`. Unit phase reached 42/43 files and 404/405 tests passing, then `tests/unit/mapLayout.test.ts` timed out on the existing "creates varied-density, standable loot across every generated landing zone" case after 30s. Process inspection showed unrelated `/home/lingchen.judy/temp/chenxuanweb/game` AlphaZero arena Node tasks consuming substantial CPU. A standalone `npm run test:unit -- tests/unit/mapLayout.test.ts` also failed to return under that load and was interrupted; no assertion failure from this visual change was observed.
- 2026-08-03: Production artifacts passed: `npm run build`, `npm run build:worker`, `npm run build:server`, and `npm run check:budgets`. Final budget results: browser entry 1,066,048/1,075,000, all browser JS 3,762,703/3,900,000, CSS 44,804/45,000, dist 4,284,803/4,450,000, Worker 449,977/450,000, standalone server 469,510/470,000. Browser entry and Worker/server budgets are very tight; no further payload growth should be added in this branch without reducing existing size.
- 2026-08-03: User clarified that heavy visual upgrades must apply only on high quality. Adjusted town presentation so dense road wetness/markings, reflective window panes, industrial lights, rooftop equipment, street furniture, overhead cables, facade pipes, weathering, roof patches, and road cracking are generated only when `quality === "high"`. Low/medium continue to avoid those batches. Added tests that low town scenes have no `town-visual-detail` batches and high town scenes have the full realistic detail set. Enhanced CombatEffects presentation with directional muzzle flashes and richer dust/spark behavior while preserving bounded pools and 0.5s cleanup behavior. `npm run typecheck` passed. `npm run test:unit -- tests/unit/islandScene.test.ts tests/unit/combatEffects.test.ts` passed: 24 tests in 123.66s.
- 2026-08-03: Rebuilt after high-only gating and action feedback changes. `npm run build`, `npm run build:worker`, `npm run build:server`, and `npm run check:budgets` passed. Latest budget results: browser entry 1,069,874/1,075,000, all browser JS 3,766,529/3,900,000, CSS 44,804/45,000, dist 4,288,629/4,450,000, Worker 449,977/450,000, standalone server 469,510/470,000. Entry budget is extremely tight, so future realism work in this branch must be payload-neutral or replace existing code.
- 2026-08-03: Browser production check with local Chrome for Testing and volume 0 reached a high-quality Greyfurnace HUD with no page console errors, saved `/tmp/last-line-visual-check.jpg`, then closed the verification page, stopped Chrome, stopped preview, and confirmed ports 9222/8798 had no listeners. WebGL `readPixels` returned transparent samples in headless despite a non-empty screenshot, so screenshot inspection remains required before final acceptance.
- 2026-08-03: Screenshot inspection showed the first high-quality pass improved the town but still looked too procedural from aircraft altitude. Added a high-quality-only industrial skyline layer with smokestacks, smoke puffs, gantry structures, and elevated pipes so the town silhouette reads more like an industrial district from distance. Tightened high-quality gating so heavy town visual batches and high-quality town lighting/materials are only active for `quality === "high"`; low/medium keep the lighter presentation. Final related validation passed: `npm run typecheck`; `npm run test:unit -- tests/unit/islandScene.test.ts tests/unit/combatEffects.test.ts` (24 tests, 122.22s); `npm run build`; `npm run build:worker`; `npm run build:server`; `npm run check:budgets`. Final budgets: browser entry 1,071,868/1,075,000, all browser JS 3,768,523/3,900,000, CSS 44,804/45,000, dist 4,290,623/4,450,000, Worker 449,977/450,000, standalone server 469,510/470,000.
- 2026-08-03: Final browser production screenshot check with local Chrome for Testing and volume 0 reached high-quality Greyfurnace HUD with no page console errors and saved `/tmp/last-line-visual-check-2.jpg`. Screenshot showed clearer storm lighting, industrial skyline silhouettes, smoke/gantry details, and dense town layout, but aircraft-phase screenshot still includes the aircraft window/pause overlay and should not be treated as full ground-level gameplay proof. Verification page was closed, Chrome and preview were stopped, and ports 9222/8798 were confirmed clear.
- 2026-08-03: Resolved Round 1 M1 by moving the existing `createTownBuildingSilhouettes` layer behind the same `qualityLevel === "high"` guard as the newer realism batches. Low-quality Greyfurnace scene tests now assert no `town-building-silhouettes-*` and no `town-visual-detail`; high-quality scene tests assert both the dense `town-visual-detail` set and the six silhouette batches.
- 2026-08-03: Revalidated after M1 fix: `npm run typecheck`; `npm run test:unit -- tests/unit/islandScene.test.ts tests/unit/combatEffects.test.ts` (24 tests, 121.23s); `npm run build`; `npm run build:worker`; `npm run build:server`; `npm run check:budgets`. Final budgets remain passing: browser entry 1,071,868/1,075,000, all browser JS 3,768,523/3,900,000, CSS 44,804/45,000, dist 4,290,623/4,450,000, Worker 449,977/450,000, standalone server 469,510/470,000.
- 2026-08-03: Attempted a real high-quality touch-mode ground-level browser check without source changes by waiting for natural auto-eject/landing. After an 80s wall-clock wait in headless Chrome, the game had advanced only to `航线部署 49%` at `1 FPS`, so ground-level proof was blocked by the current headless/software WebGL environment and external CPU load. The check still confirmed volume 0, no page console errors, and saved `/tmp/last-line-real-ground-check.jpg`; Chrome and preview were stopped and ports 9222/8798 were confirmed clear. This does not close the full ground-level visual acceptance gap by itself.

## Review

- 2026-08-03 Round 1 required reviewer: Static diff review only. Read this plan, inspected `git diff --stat`, `git diff --name-only`, targeted `IslandScene`, `CombatEffects`, and `islandScene` test hunks, and ran read-only `git diff --check`. Did not rerun full typecheck/test/build/budget/browser commands already recorded above.
- Finding M1 open: Heavy town realism is not fully high-only. `createTownBuildingSilhouettes(scene, layout)` is called unconditionally before the `qualityLevel === "high"` guard, and the function adds factory stacks, warehouse roof monitors, rowhouse utilities, commercial roof signs, tower crowns/antennas, and corner pavilions for every town building before merging them into `town-building-silhouette` batches. The low-quality Greyfurnace test now explicitly expects those batches and relaxes the scene mesh cap from 900 to 930, so low/medium still receive a new realism layer despite the high-only requirement. Move this silhouette layer behind the high-quality path or document and test why it is considered lightweight enough for low/medium while keeping the resource assertion meaningful. Evidence: `src/client/render/scenes/IslandScene.ts:708`, `src/client/render/scenes/IslandScene.ts:1095`, `tests/unit/islandScene.test.ts:960`, `tests/unit/islandScene.test.ts:1008`.
- Finding M2 open: Final acceptance evidence is incomplete for the "extremely realistic" visual target. The plan's final browser evidence records only an aircraft-phase high-quality Greyfurnace screenshot and explicitly says it includes the aircraft window/pause overlay and should not be treated as full ground-level gameplay proof. That cleanup record is good, but before completion the branch still needs acceptance evidence from high-quality ground-level gameplay, or a plan note narrowing acceptance to aircraft-phase visuals. Evidence: `.agents/plans/2026-08-03-overwatch-grade-visuals.md:17`, `.agents/plans/2026-08-03-overwatch-grade-visuals.md:36`.
- 2026-08-03 Round 2 required reviewer: Static/read-only re-review of the updated plan and current diff. Rechecked the M1 call site, high/low test expectations, current changed file set, `git diff --check`, and `src/game` diff absence. Did not rerun full typecheck/test/build/budget/browser commands already recorded above.
- Finding M1 resolved: `createTownBuildingSilhouettes(scene, layout)` now runs inside the same `qualityLevel === "high"` guard as the dense town realism batches, while the low-quality Greyfurnace test asserts no `town-building-silhouettes-*` and no `town-visual-detail`, and the high-quality test asserts both the dense `town-visual-detail` set and the six silhouette batches. Evidence: `src/client/render/scenes/IslandScene.ts:708`, `tests/unit/islandScene.test.ts:960`, `tests/unit/islandScene.test.ts:1032`, `tests/unit/islandScene.test.ts:1058`.
- Finding M2 resolved as documented environment limitation: The branch still lacks a successful ground-level high-quality screenshot, but the plan now records a real high-quality touch-mode attempt that kept volume at 0, reached no page console errors, saved `/tmp/last-line-real-ground-check.jpg`, and cleaned up Chrome/preview/ports. The attempt was blocked by the current headless/software WebGL and external-load environment, advancing only to `航线部署 49%` at `1 FPS` after 80s wall time. With no actionable static evidence that the implementation itself fails the visual target, treat this as an accepted residual verification gap rather than an unresolved medium. Evidence: `.agents/plans/2026-08-03-overwatch-grade-visuals.md:39`.
- Round 2 result: no unresolved blocker, high, or medium findings.
